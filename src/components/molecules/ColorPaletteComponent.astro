---
import ColorCircleComponent from "../atoms/ColorCircleComponent.astro";

const { paletteName, colors } = Astro.props;
console.log(paletteName, colors);
---
<div class="bg-white p-4 rounded-lg shadow-md">
  <!-- Nombre de la paleta de colores -->
  <h3 class="text-xl font-bold text-secondary text-center mb-4">{paletteName}</h3>

  <!-- Fila de círculos -->
  <div class="flex space-x-4 justify-center">
    {colors.map((color: any) => (
      <ColorCircleComponent hexCode={color.hex} />
    ))}
  </div>
</div>
