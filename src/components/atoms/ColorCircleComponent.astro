---
const { hexCode } = Astro.props;
console.log(hexCode);
---
<div class="flex flex-col items-center">
  <!-- Círculo de color -->
  <div class="w-16 h-16 rounded-full border border-gray-300" style={`background-color: black;`}>x</div>
  
  <!-- Código HEX -->
  <p class="mt-2 text-secondary">{hexCode}</p>
</div>
